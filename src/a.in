
int N = 15;
bool[] b = new bool[15001];

int main()
{
  int i;

  for (i = 1; i <= N; i++) b[i] = true;

  for (i = 2; i <= N; i++) if (b[i])
  {
    int count = 2;
    while (i*count <= N)
    {
      b[i*count] = false;
      count++;
    }
  }

  return 0;
}
